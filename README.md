# horizontalsystems git-maven repository

Built artifacts of the Zodl Zcash Android SDK (branch hs/3.1.x, Slipstream enabled).

Consume with:

    maven { url = uri("https://raw.githubusercontent.com/horizontalsystems/zodl-android-wallet-sdk/maven/") }
